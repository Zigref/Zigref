# Zigref

[Zigref Website](https://zigref.dev)
> Documentation of every zig package on Zigistry.
 That only contains the documentation that a package
 user would require
 
This is not like emit docs, this only reads identifiers marked with `pub`, even those that are nested within a `pub` container.
